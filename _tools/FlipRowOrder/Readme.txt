Replacement for "Flip row order", which can only be found in Photoshop.
It specifies the image height as a negative value, 
instructing browsers to interpret the image from top to bottom, 
while also flipping the data, since PicoPad ignores the BMP file header.
